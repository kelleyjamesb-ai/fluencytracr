import type { PatternInferenceRecord } from "./types";

export const MIN_DAYS = 0;
export const MIN_EVENTS = 0;

export const gateInference = (
  record: PatternInferenceRecord
): PatternInferenceRecord => {
  return record;
};
