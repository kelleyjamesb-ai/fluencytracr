import type { EnablementEventRecord, EnablementRollupRecord } from "./store";

export const runDailyEnablementRollup = (_orgId: string, _day: string): EnablementRollupRecord[] => {
  return [];
};

export const runEnablementRollupsForEvents = (_orgId: string, _events: EnablementEventRecord[]): null => {
  return null;
};
