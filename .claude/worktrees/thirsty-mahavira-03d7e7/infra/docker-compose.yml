version: "3.9"

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: fluency
      POSTGRES_PASSWORD: fluency
      POSTGRES_DB: fluency
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
