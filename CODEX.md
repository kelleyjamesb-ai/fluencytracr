# Codex Entry Point

Read `AGENTS.md` first. It is the canonical repository instruction file for
all agents and contributors.

For long-running or multi-session work, also read
`docs/agent/SESSION_START.md`.

This file is intentionally a pointer, not a duplicate policy source.
