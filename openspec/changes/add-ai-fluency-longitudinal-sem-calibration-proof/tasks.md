## 1. Frozen Measurement Boundary

- [x] 1.1 Freeze the exact 24-item long-form manifest, construct roles,
  ordered anchors, source hash, second-order structure, and no-reverse-code
  policy.
- [x] 1.2 Pin the 14-item form as pulse-only and the six behavior questions as
  separate non-scoring pathway/corroboration evidence.

## 2. Aggregate Evidence Contract

- [x] 2.1 Implement strict aggregate-only item and pair-count evidence types,
  validation, canonical hashing, and two-wave package binding.
- [x] 2.2 Reject missing pairs/items, duplicate/off-plan cells, incompatible
  forms, sparse evidence, bad hashes, unsafe fields/values, respondent data,
  behavior fields, real/live/customer/production flags, and output
  authorization attempts.
- [x] 2.3 Add a source-side Google Apps Script adapter and focused Node tests
  proving only privacy-safe aggregate sufficient statistics leave the
  workbook.

## 3. Synthetic Ordinal Measurement Proof

- [x] 3.1 Add deterministic synthetic two-wave ordinal data generation for the
  frozen eight-construct and second-order structure.
- [x] 3.2 Fit the aggregate ordinal-probit composite measurement proof and
  compute reliability, configural, loading-stability, threshold-stability,
  and parameter-recovery diagnostics without emitting respondent scores or
  latent states.
- [x] 3.3 Add invariant, invariant-latent-shift, loading-drift,
  threshold-drift, wrong-form, sparse, incomplete, unsafe, and hash-invalid
  controls.
- [x] 3.4 Add a fixed full 200-replication-per-scenario study plan and reduced
  smoke mode; incomplete or smoke evidence MUST HOLD and artifact emission
  MUST freshly recompute every planned slot.
- [x] 3.5 Feed computed study output into a hash-bound, summary-only,
  synthetic/internal/non-authorizing calibration artifact.

## 4. Verification And Review

- [x] 4.1 Run focused and full Python inference tests.
- [x] 4.2 Run the Google Apps Script adapter test and relevant aggregate-export
  regression test.
- [x] 4.3 Run confidence-engine/shared builds and relevant bridge tests if the
  bridge boundary changes.
- [x] 4.4 Run strict OpenSpec validation, docs/semantic/V1 governance checks,
  harness verification, and `git diff --check`.
- [x] 4.5 Obtain independent CODE, BUG, and ADVERSARIAL acceptance and resolve
  every blocking finding.
- [x] 4.6 Update queue/progress and calibration contract status only after all
  required verification passes.

## 5. Explicitly Incomplete Future Work

- [ ] 5.1 Execute and independently accept the full >=200-replication
  measurement calibration evidence set.
- [ ] 5.2 Equate the 14-item pulse form, if a later proposal justifies it.
- [ ] 5.3 Admit and calibrate governed real aggregate evidence under a separate
  source-binding and promotion decision.
- [ ] 5.4 Estimate Attitude/Intent/Impact structural paths after measurement
  invariance is accepted.
- [ ] 5.5 Mark parent task `5.5` complete only after full evidence execution and
  exact-byte independent acceptance.
