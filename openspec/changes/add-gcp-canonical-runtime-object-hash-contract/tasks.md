## 1. Governance and scope

- [x] 1.1 Bind the change to merged Section 7.1 provider-vocabulary artifacts and the user-approved Section 7.2 scope.
- [x] 1.2 Record runtime authority as held and prohibit GCP/resource/runtime/customer actions.
- [x] 1.3 Document the architecture, alternatives, privacy boundary, and downstream ownership.

## 2. Contract artifacts

- [x] 2.1 Define the deterministic numerical-profile and per-instance observation field registries.
- [x] 2.2 Define the total Compute projection, identity-keyed resource representation, and profile/control crosswalk.
- [x] 2.3 Define strict canonical serialization, domain-separated hash preimages, and the acyclic Section 7.2 graph.
- [x] 2.4 Define visibility, sufficiency, chronology, requalification, and fixed-physical escalation decisions.
- [x] 2.5 Record fresh provider revalidation evidence and synthetic golden vectors.

## 3. Verification

- [x] 3.1 Add the offline external-source replay verifier with exact artifact and bundle pins.
- [x] 3.2 Add focused mutation and regression tests for every new contract behavior.
- [x] 3.3 Run focused and full Python suites, docs sweep, V1 governance, external replay, JSON/compile/diff checks, and independent reviews.
- [x] 3.4 Validate this OpenSpec change with `openspec validate add-gcp-canonical-runtime-object-hash-contract --strict`.

## 4. Deferred work

- [x] 4.1 Confirm Sections 7.3–7.8, GCP qualification, deployment, model execution, and runtime implementation remain outside this change.
