## 1. Contract

- [x] 1.1 Add the pure aggregate disclosure policy, bounded privacy-equation
      domain types, prior-receipt input, and non-canonical internal diagnostics.
- [x] 1.2 Add contract documentation and additive schemas for exact slice,
      fixed-window, cohort, hierarchy, and atomic-child privacy context.
- [x] 1.3 Require server-owned immutable completeness/lineage/source manifests,
      stable non-person privacy slots, canonical content fingerprints, and one
      temporal grid; caller assertions remain non-authoritative.
- [x] 1.4 Preserve the nine canonical events, five suppression reasons, fixed
      thresholds, and storage-only legacy compatibility.

## 2. Rollup And Replay Authority

- [x] 2.1 Route generic metric and behavioral direct/connector rollups through
      complete disjoint partitions.
- [x] 2.2 Hold every parent containing a suppressed, unknown, ambiguous, or
      storage-only child and prevent caller-parent/derived-child collisions.
- [x] 2.3 Atomically persist the server-owned privacy replay receipt with its
      admitted projection; permit identical replay while holding partial,
      changed, mixed-source, cross-axis, overlapping, adjacent, rolling, and
      multi-window disclosure equations.
- [x] 2.4 Evaluate aliases and algebraic dependencies across counts, totals,
      percentages, ratios, shares, bands, concentrations, axes, and sources in
      one collision domain.

## 3. Read And Derived Surfaces

- [x] 3.1 Apply the admitted projection before dashboard and behavioral query
      filters and before pattern/orientation/visibility derivation.
- [x] 3.2 Remove aggregate values, cohort sizes, evidence identifiers, and exact
      count splits from suppressed Outcome Evidence and observability outputs.
- [x] 3.3 Preserve independently suppressed workflow/JBTD/persona slices and
      resolve stable canonical opaque contribution tokens across modes,
      aliases, retries, workers, and restarts; missing or duplicate authority
      HOLDs.
- [x] 3.4 Apply the same projection to enablement and spread outputs; regression
      test already-safe V3 and Velocity paths without broadening their design.

## 4. Verification

- [x] 4.1 Add fail-first transcript-equivalence tests for complementary
      suppression, direct/connector parity, aliases, filters, hidden
      membership/count changes, false completeness, and partial replacement.
- [x] 4.2 Add adjacent/overlapping-window and suppressed-value projection
      regression tests across every affected read, import, pagination, cache,
      audit, debug, and derived surface, including enablement/spread count and
      ratio equations.
- [x] 4.3 Add durable replay tests across restart, multiple principals/workers,
      and concurrent conflicting direct/connector writes; prove at most one
      candidate can affect a privacy slot and five representations of one
      contribution cannot satisfy the cohort gate.
- [x] 4.4 Run focused shared/backend tests, applicable builds, Assurance
      Harness, governance checks, strict OpenSpec validation, and exact
      CODE/BUG/ADVERSARIAL review.
