## 1. Contract and fail-first evidence

- [x] 1.1 Add the strict aggregate claim-authorization manifest, bounded
      movement template, and fixed held response contracts.
- [x] 1.2 Add fail-first tests for adjacent-gate, receipt-only, cross-slice,
      alternate-pair, multiple-movement, arithmetic-overflow, concurrent
      mutation, internal-type exposure, compatible-looking source-graph
      substitution, same-ID source mutation, forged readiness lineage,
      artifact substitution, revocation, redacted hold, and blocked-language
      attacks.
- [x] 1.3 Validate this OpenSpec change strictly.

## 2. Server authority and immutable binding

- [x] 2.1 Make real-evidence materialization persist its generated scenario and
      seal the review-independent immutable export evidence content plus exact
      blueprint/metrics/scenario IDs and canonical hashes into
      materializer-only authoritative readiness validation.
- [x] 2.2 Add server-owned claim authorization from accepted authoritative
      evidence/readiness, the sealed readiness-lineage source graph, plus one
      exact C.1 readback projection; request IDs are equality selectors only.
- [x] 2.3 Add non-circular manifest-derived internal IDs, domain-separated
      canonical hashing, and atomic insert-or-exact bundle persistence against
      a locked serializable source snapshot without a database migration.
- [x] 2.4 Route one-movement value-chain persistence through the authority and
      require post-commit C.1 plus source/artifact/manifest reconciliation
      before returning authorization.

## 3. Bypass demotion and readback

- [x] 3.1 Demote generic spine and direct object writes from claim authority,
      and exclude reserved internal artifact types from generic create, read,
      list, and upsert paths.
- [x] 3.2 Require deterministic packet-to-manifest lookup plus exact current
      source, manifest, artifact, policy-state, and C.1 reconciliation before
      HTML readout rendering.
- [x] 3.3 Preserve additive upstream storage/calculation compatibility while
      returning fixed redacted held responses with no claim material,
      including for `persist: false`.

## 4. Verification and closeout

- [x] 4.1 Run focused shared/backend claim, value-chain, C.1, and readout
      tests.
- [ ] 4.2 Run shared/backend builds, full backend tests, V1 governance,
      Assurance Harness, docs sweep, and strict OpenSpec validation.
- [ ] 4.3 Obtain exact-candidate CODE/BUG/ADVERSARIAL review and update durable
      queue/progress state.
