## 0. Queue Admission

- [x] 0.1 Human directs addition of the exact bounded high-risk queue item to
  `.project/WORK_QUEUE.json`.
- [x] 0.2 Agent marks only that human-authored item `in_progress` and records the
  exact bound in `.project/PROGRESS.md`.

## 1. Step 5: Aggregate Implementation

- [x] 1.1 Freeze model constants, panel geometry, lags, seeds, sampler settings,
  validation limits, and sanitized artifact shape.
- [x] 1.2 Implement strict aggregate-only analysis-plan, checkpoint, transition,
  capability, outcome, control, alignment, and freeze types.
- [x] 1.3 Implement deterministic preparation and hierarchical hash bindings.
- [x] 1.4 Implement one-slice-per-panel synthetic fixtures with disjoint family
  registries and no cross-slice output.
- [x] 1.5 Add fail-closed structural and privacy tests.

## 2. Step 6: Joint Model Implementation

- [x] 2.1 Implement the family-state transition and capability measurement
  components.
- [x] 2.2 Implement the continuous-normal outcome component with frozen lags,
  behavior terms, capability moderation, controls, and time structure.
- [x] 2.3 Implement the frozen no-behavior restricted model.
- [x] 2.4 Implement sanitized internal summaries and reject unsafe output fields.
- [x] 2.5 Add focused model, hashing, diagnostics, and artifact tests.

## 3. Step 7: Synthetic Validation

- [x] 3.0 Freeze the V4 replicated validation protocol, including scenarios,
  replicate and fit manifests, data and sampler seeds, numerical gates,
  nonqualifying preflights, runtime limits, output boundary, and review gates.
- [x] 3.1 Implement the V4 replicated validation plan, generator cases,
  immutable create-once claims, append-only attempt ledger, separate preflight,
  canary, and qualifying manifests, external checkpoint runner, exact combiner,
  closed failure codes, and sanitized ensemble artifact.
- [ ] 3.2 Run lag, confounding, capability-error, source-drift,
  cohort-alignment, algebraic-duplication, availability-state, and binding
  controls.
- [ ] 3.3 Obtain independent technical, adversarial, and statistical
  pre-execution review of the exact implementation commit and runtime manifest.
- [ ] 3.4 Run the frozen six-fit smoke preflight and paired full-setting runtime
  canary. Both remain permanently nonqualifying.
- [ ] 3.5 Run and combine the exact 400-dataset, 600-fit qualifying universe.
- [ ] 3.6 Independently recompute the ensemble summary and verify its exact
  sanitized output.
- [ ] 3.7 Obtain independent technical, adversarial, and statistical review of
  the complete study.

## 4. Step 8: Integration Decision

- [x] 4.1 Record the evidence state and unresolved limitations.
- [x] 4.2 Select `HOLD_FOR_MODEL_REPAIR` for the nonqualifying V1 diagnostic
  evidence.
- [x] 4.3 Keep real data, runtime, product, and customer-output work pending unless
  a later exact-scope proposal is separately approved.
- [ ] 4.4 Complete the human-authored queue item only after all authorized tasks
  and required verification are complete.
