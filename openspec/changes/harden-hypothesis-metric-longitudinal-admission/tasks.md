## 1. Admission Contract

- [x] 1.1 Define a company metric catalog with no arbitrary product-level count cap and no automatic model eligibility.
- [x] 1.2 Define one immutable primary-metric analysis unit per approved hypothesis plan.
- [x] 1.3 Bind metric definition ref/hash, one-tuple-per-panel manifests and gate receipts, model/control/exposure/baseline-Fluency hashes, family, unit, denominator, transformation, aggregate grain, uncertainty, windows, lag, source, ownership, approval, hash-bound pre-outcome access receipt, and terminal-look posture.
- [x] 1.4 Preserve supporting, guardrail, AI Fluency, Velocity, Breadth, and Depth evidence roles without blending or substitution.
- [x] 1.5 Keep current metric-library, intake, Measurement Plan, and Measurement Cell readiness states non-admissive and preserve the existing recommendation allowlist.

## 2. Model And Portfolio Gates

- [x] 2.1 Limit current eligibility to the exact `continuous_normal_identity` token and accepted 12-pre/6-post, 6-or-12-group, aggregate-`k=16` replicated envelope with known positive uncertainty and all existing gates.
- [x] 2.2 HOLD unsupported metric families without creating a canonical suppression reason or selecting a second estimator.
- [x] 2.3 Require pre-outcome frozen planned-unit accounting, separate evidence and fit dependency keys, prepared-input/fit-summary result bindings, exact manifest/result equality, explicit HOLD records, and one fixed terminal look across multiple hypotheses.
- [x] 2.4 Separate descriptive selected-metric movement, standardized associational longitudinal contrast, reporting movement state, and the prebound categorical evidence-design claim cap; block numeric AI-attribution confidence.

## 3. Non-Authorization

- [x] 3.1 Preserve docs-only, aggregate-only, noncausal, nonauthorizing boundaries and every FluencyTracr invariant.
- [x] 3.2 Keep measurement-model calibration, VBD trajectory calibration, DiD completion, real-data admission, runtime routing, reporting, persistence, and UI as separate future work.
- [x] 3.3 Reconcile canonical metric-family and accepted synthetic-proof metadata, and pin it with contract-test coverage, without changing executable behavior.

## 4. Verification

- [x] 4.1 Run strict OpenSpec validation for this change, the model-family change, and the formula-registry change.
- [x] 4.2 Run docs sweep, semantic drift guard, V1 governance gates, Assurance Harness, and `git diff --check`.
- [x] 4.3 Complete independent CODE, BUG, and ADVERSARIAL review before status completion.
