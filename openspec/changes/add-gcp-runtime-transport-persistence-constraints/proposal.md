# Change: Add Section 7.5A transport and persistence constraints

## Why

Attempting to define a positive Section 7.5 interface exposed two cross-section prerequisites: Section 7.3 has no admitted mechanism/network/anchor capabilities, and Section 7.4 has no external typed approval/current-target lineage outside compile-pinned bytes. Section 7.5A must freeze the known negative constraints and exact debt without pretending those prerequisites are closed.

## What Changes

- Add a machine-distinct docs-only constraints/open-obligations contract that cannot satisfy full Section 7.5 or become a Section 7.6 dependency.
- Bind exact Section 7.2–7.4 obligations, 53 public documentation sources, and a 89-row method research inventory.
- Freeze network, local, GCS, Spanner, audit, privacy, and authority constraints.
- Freeze 20 bidirectionally cross-referenced open prerequisites and a mandatory runtime HOLD.
- Add no verified record, writer, approval path, readiness packet, test, validator, SUT, runtime interface, or implementation.

## Impact

- New docs contract: `docs/contracts/canonical-inference-gcp-transport-persistence-constraints/`
- New spec: `gcp-runtime-transport-persistence-constraints`
- Attribution and queue/progress metadata only
- No product/runtime/API/schema implementation

## Non-Authorization

This change is not full Section 7.5, `READINESS_GO`, implementation approval, provider capability proof, or runtime authority. It authorizes no GCP, credentials, signing, provisioning, deployment, persistence, evidence collection, qualification, model execution, or Sections 7.6–7.8 work.
