## 1. Methodology Direction

- [x] 1.1 Approve VBD family-state movement as the observed aggregate
  human-behavior pathway between stated capability and customer-owned outcomes.
- [x] 1.2 Keep the three evidence lanes separately measured. An overall
  weighted score is prohibited.
- [x] 1.3 Set the default interpretation to associative and predictive rather
  than causal.

## 2. Governance Reconciliation

- [x] 2.1 Preserve the nine canonical events, five suppression reasons,
  aggregate-only boundary, independent slice gates, and default SUPPRESS.
- [x] 2.2 Hold function drilldowns, cross-slice rescue, unresolved derived-scope
  joins, Depth likelihood use, and all person-level or ranking outputs.
- [x] 2.3 Version the legacy frequency, engagement, and Breadth trajectory as a
  different held research estimand that cannot be relabeled.
- [x] 2.4 Preserve the current continuous-normal outcome eligibility and
  pre-outcome freeze boundary.

## 3. Aggregate Data Contract

- [x] 3.1 Define immutable analysis-plan, VBD checkpoint, VBD transition,
  stated-capability, outcome, control, and alignment records.
- [x] 3.2 Define exact count identities for Eligible, Active, Embedded,
  retained, newly embedded, lapsed, and remaining nonembedded states.
- [x] 3.3 Require stable eligibility, source coverage, semantic policy, cadence,
  finality, revision, and transition-intersection evidence.
- [x] 3.4 Require exact cross-lane cohort, workflow-family, window, exposure,
  lag, source, and evidence-revision alignment.

## 4. Joint Bayesian Methodology

- [x] 4.1 Define the aggregate retention, new-embedding, and
  active-nonembedded family-state component.
- [x] 4.2 Define the aggregate stated-capability measurement component with
  required uncertainty and a predeclared temporally ordered
  capability-to-behavior association.
- [x] 4.3 Define the continuous-normal outcome component with predeclared lags,
  controls, time structure, and capability-by-behavior moderation.
- [x] 4.4 Define uncertainty propagation and prohibit algebraically dependent
  predictors.
- [x] 4.5 Define frozen full-versus-restricted predictive comparison and bound
  Bayesian R-squared and future-window fit to internal noncausal use.
- [x] 4.6 Define estimands, claim ceilings, HOLD behavior, and non-authorization.

## 5. Future Aggregate Implementation

- [x] 5.1 Record James Kelley's 2026-08-11 approval to proceed with steps 5
  through 8, bounded to synthetic aggregate implementation, validation, and a
  governed integration decision.
- [ ] 5.2 Resolve family-to-canonical-slice allocation and derived-scope privacy.
- [ ] 5.3 Define runtime schemas and deterministic preparation.
- [ ] 5.4 Add aggregate-only synthetic family-state fixtures and fail-closed
  structural tests.

## 6. Future Joint Model Implementation

- [ ] 6.1 Freeze priors, model geometry, numerical settings, and seed universe.
- [ ] 6.2 Implement the joint model under a separately approved execution gate.
- [ ] 6.3 Emit only internal sanitized summaries with no posterior draws,
  latent paths, customer output, or product authority.

## 7. Future Joint Model Validation

- [ ] 7.1 Run synthetic recovery, calibration, sensitivity, negative-control,
  and full-versus-restricted predictive validation.
- [ ] 7.2 Complete independent acceptance and record an explicit promotion or
  HOLD decision.

## 8. Future Product Decision

- [ ] 8.1 Decide whether any real aggregate data path is authorized.
- [ ] 8.2 Decide whether any runtime service, API, persistence, or internal
  readout is authorized.
- [ ] 8.3 Keep customer-facing probability, causal impact, productivity, ROI,
  ranking, and economic output blocked unless a later contract promotes that
  exact scope.
