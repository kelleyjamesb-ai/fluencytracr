name: Agent CI

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Set up Python 3.12
      uses: actions/setup-python@v6
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run V1 governance gates
      run: python scripts/ci_v1_governance_gates.py

    - name: Run Tests
      run: |
        # Section 7.5.1 future-SUT cases are an explicit intentional-red audit
        # until the separately authorized evaluator exists.
        pytest tests/ -k "not test_future_sut_attack_case and not test_future_sut_environment_cell"
