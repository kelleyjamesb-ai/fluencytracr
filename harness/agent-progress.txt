# Agent progress log (append only)
#
# Each session: add a new block with date (UTC), tool (Codex / Claude / other), and bullets:
# - What you completed
# - What you verified (tests, manual smoke)
# - Next recommended focus (one feature id from feature_list.json)
#
# ---

## 2026-03-31 (UTC) — Cursor agent session

- Completed: Added `docs/agent/` hub (README, TASK_CONTRACT, EVALUATION), `harness/scripts/verify.sh` (Python unittest parity with CI), `artifacts/harness/checkpoints/.gitkeep`, expanded `AGENTS.md` / `CLAUDE.md` / root `README` pointers, updated harness prompts and `feature_list.json` with verification + OpenSpec items.
- Verified: Repo inspection only; full `verify.sh` requires local `pip install -r requirements.txt` (and pytest where tests import it).
- Next focus: `harness-002` after deps installed, or `harness-003` when touching TS workspaces.

## 2026-03-31 (UTC) — Cursor (principal architect + harness)

- Completed: Defined **harness roles** (architect, schema evaluator, privacy reviewer, integration owner, implementation owner). Added unified telemetry contract `docs/contracts/unified-telemetry/README.md` (four categories, naming, versioning, streaming, FT_V1 mapping) and `schemas/unified_telemetry/ut_event_union.schema.json`. Added `harness-006` for downstream ingest wiring.
- Verified: Schema structure reviewed; JSON Schema `allOf` + `additionalProperties` conflict removed from envelope core.
- Next focus: `harness-006` for zod/backend/CI, or `harness-004` if ingest changes need OpenSpec first.

## 2026-03-31 (UTC) — Cursor (OpenSpec + UT ingest)

- Completed: OpenSpec `openspec/changes/add-unified-telemetry-ingest/`; `shared/src/unifiedTelemetrySchemas.ts`; `POST /api/ingest/unified-telemetry` behind `FLUENCY_UNIFIED_TELEMETRY_INGEST`; `store.unifiedTelemetryEvents`; `backend/tests/unified_telemetry_ingest.test.ts`; `docs/api/ingest-unified-telemetry.md`.
- Verified: Pending local `npm run test:ci --workspace backend` (sandbox without npm).
- Next focus: Run backend tests; set `harness-006` passes true after green CI; archive OpenSpec change post-deploy.

## 2026-03-31 (UTC) — James (local)

- Verified: `npm run test:ci --workspace backend` — 49 suites, 252 tests passed (includes unified telemetry ingest).
- Harness: set `harness-006` `passes` to true.
- Next focus: `harness-003` if touching frontend/shared; archive OpenSpec `add-unified-telemetry-ingest` after deploy; optional `harness-002` (Python verify).

## 2026-03-31 (UTC) — Cursor (PRD v1 + harness)

- Completed: Landed **PRD v1** as `artifacts/PRD_V1_BEHAVIORAL_OBSERVABILITY.md` (full product definition + execution FSM, signals, patterns, baselines, open questions). Added **Appendix A** (repo alignment: `/api/events`, `/api/ingest`, unified telemetry vs PRD observability API) and **Appendix B** (harness + OpenSpec gate). Cross-linked §4 data inputs to `artifacts/DATA_ACCESS_CONTRACT_RFI.md` and unified telemetry docs.
- Harness: Added `prd-doc-001` (**passes: true**), `prd-phase-01`…`prd-phase-04` (**passes: false**) to `harness/feature_list.json`. Linked PRD from `docs/agent/README.md`. Ran `./harness/scripts/bootstrap.sh` (OK).
- Verified: Artifact present; JSON valid; bootstrap exit 0.
- Next focus: **`prd-phase-01`** when starting engine work (OpenSpec **`harness-004`** if ingest/schema/API contracts change); else continue **`harness-002`** / **`harness-003`** per touch surface.

## 2026-04-01 (UTC) — Cursor (PRD Phase 1 implementation)

- Completed: **PRD Phase 1 core engine** — optional correlation fields on fluency events (`run_id`, `workflow_run_id`, `agent_run_id`, `chat_id`); `resolveFluencyExecutionId` + `buildFluencyEventRecord`; `trace_engine` (sort, 15m retry pairs, stage groups, empty tool groups); `POST /api/events` returns `execution_ids`; `GET /api/traces/reconstructed` (ADMIN/ENABLEMENT_LEAD); `/api/ingest` fluency path uses builder; seed uses builder. OpenSpec change `openspec/changes/add-prd-phase1-core-engine/`. Doc `docs/api/traces-reconstructed.md`.
- Verified: `npm run build --workspace shared` and `npm run test:ci --workspace backend` (full suite + contracts) pass locally.
- Harness: `prd-phase-01` set **`passes: true`**.
- Next focus: **`prd-phase-02`** (signal + pattern layer per PRD §14–15) behind governance review; archive OpenSpec change post-review/deploy.

## 2026-04-01 (UTC) — Cursor (PRD Phase 2)

- Completed: **Execution signals + pattern classification** — `backend/src/execution_signals.ts` (`SIGNAL_REGISTRY`, `computeExecutionSignals`, `classifyExecutionPattern`, default thresholds). `GET /api/traces/reconstructed?include_signals=true` returns `signals`, `pattern`, `pattern_confidence_tier`. OpenSpec `openspec/changes/add-prd-phase2-execution-signals/`. Tests: `execution_signals.test.ts`, contracts. Docs: `docs/api/traces-reconstructed.md`.
- Verified: `npm run test:ci --workspace backend` (274 tests).
- Note: Workflow-relative baselines (PRD §16) not yet applied to thresholds — defaults only.
- Harness: `prd-phase-02` **`passes: true`**.
- Next focus: **`prd-phase-03`** (suppression + API constraints) or wire baselines per workflow without trend surfaces.

## 2026-04-01 (UTC) — Cursor (PRD Phase 3)

- Completed: **Execution disclosure** — `backend/src/execution_disclosure.ts` (`evaluateExecutionDisclosure`, `applyDisclosureToTraces`). Fail-closed: `insufficient_event_count`, `low_confidence_tier`, `invalid_timestamps`. `include_signals=true` responses include `disclosure`; when `SUPPRESSED`, `signals`/`pattern`/`pattern_confidence_tier` are null. Env `FLUENCY_MIN_EXECUTION_EVENTS_FOR_DISCLOSURE`. OpenSpec `add-prd-phase3-execution-disclosure`. `backend/.env.example` note.
- Verified: `npm run test:ci --workspace backend` (281 tests).
- Harness: `prd-phase-03` **`passes: true`**.
- Next focus: **`prd-phase-04`** (workflow-level observability API) or workflow-relative thresholds (PRD §16) without trend leakage.

## 2026-04-01 (UTC) — Cursor (PRD Phase 4)

- Completed: **`GET /api/observability/:orgId`** — `buildObservabilityRollup` (`observability_aggregate.ts`): org-scoped + windowed events, per-execution Phase 2/3 pipeline, workflow-level `pattern_distribution` (fixed keys), cohort suppression via `MIN_COHORT_SIZE`, `allowed_interpretation_hints` only when workflow `ALLOWED`. Shared Zod `ObservabilityResponseSchema`. Tests: `observability_aggregate.test.ts`, `observability_api.test.ts`. Doc `docs/api/observability-org.md`, README link. OpenSpec `add-prd-phase4-observability-api`.
- Verified: `npm run test:ci --workspace backend` (290 tests).
- Harness: `prd-phase-04` **`passes: true`**.
- Next focus: PRD §16 workflow-relative thresholds (no trend leakage), persistence beyond in-memory store, or product hardening.

## 2026-04-01 (UTC) — Cursor (v1 boundary adaptation)

- Completed: **Ingest boundary layer** — `backend/src/boundary/*` (actor-type adapter with explicit tables, execution-id resolver with opt-in composite `workflow_id::chat|agent`, Zod `upstreamIngestEventSchema` + `parseUpstreamIngestEvent`, `mapUpstreamEventToCanonical` with `upstream_lineage` metadata, `app-integration.example.ts`). `v1-pipeline-types` delegates actor mapping to `mapActorType` and supports `actor` / `actor_type` precedence. Plan: `artifacts/plan_boundary_adaptation_v1.md`.
- Verified: `npm run test:ci --workspace backend` (75 suites, 429 tests).
- Harness: **`prd-boundary-v1`** marked **`passes: true`**.
- Next focus: Wire `exampleProcessIngestBody` (or equivalent) into production `POST /api/events` behind flag if desired; keep canonical validation strictly downstream of boundary.

## 2026-04-01 (UTC) — Cursor (harness green)

- Completed: **Full harness verification** — fixed merge-corrupted Python (`src/memory.py` import/save_memory, `src/sandbox/factory.py` except branches, `tests/test_factory.py`). `harness/scripts/verify.sh` now runs `pip install -r requirements.txt` + `python3 -m pytest tests/ -q` (mixed unittest + pytest modules). `.github/workflows/ci.yml` python-tests aligned. `docs/agent/EVALUATION.md` updated.
- Verified: `./harness/scripts/verify.sh` (138 passed, 3 skipped); `python3 scripts/ci_v1_governance_gates.py`; `npm run build --workspace shared`; `npm run test:ci --workspace backend` (429 tests); `npm test --workspace frontend` (13 tests).
- Harness: **`harness-001`–`harness-004`** set **`passes: true`** in `feature_list.json`.
- Next focus: Optional CI speed (cache pip/npm); wire boundary example into production routes if product asks.

## 2026-04-01 (UTC) — Cursor (session protocol doc)

- Completed: Canonical **every-session** doc [`docs/agent/SESSION_START.md`](../docs/agent/SESSION_START.md) (Anthropic-style harness + `.project/` queue, memory table, roles, verification). Linked from root `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/agent/README.md`, `harness/README.md`, `harness/prompts/*`, `agents/core/SYSTEM_PROMPT.md`, `.cursorrules`.
- Next focus: Human adds/curates `WORK_QUEUE.json` items; agents follow `SESSION_START` → one bounded unit per session.

## 2026-04-01 (UTC) — Cursor (GitHub Actions Node 24 readiness)

- Completed: Bumped workflows to `actions/checkout@v5`, `actions/setup-node@v6`, `actions/upload-artifact@v5` (ci node job); `setup-python@v5` + `checkout@v5` in `test.yml`; `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true` on workflows using JS actions; **Node 24** for npm/Prisma in `ci.yml` / `governance-gate.yml`.
- Note: Vercel deployment failures are project access / CLI config — not fixed by workflow YAML alone.

## 2026-05-01 (UTC) — Codex (OpenAI Agents SDK harness)

- Completed: Added optional `integrations/openai-agents/` sidecar using `@openai/agents`, repo harness document tools, manager/specialist agents-as-tools, `npm run agent:dev`, and `npm run validate:agents`. Linked docs from README, `docs/agent/README.md`, `docs/agent/EVALUATION.md`, and `harness/README.md`; added CI validation.
- Verified: `npm run validate:agents`; `bash scripts/ci_docs_contract_sweep.sh`; `bash scripts/ci_linkcheck_fluency_docs.sh`. `npm audit --omit=dev` shows existing advisories in `@modelcontextprotocol/sdk`/`hono` and frontend `mammoth`/`xmldom`, not the new OpenAI sidecar.
- Next focus: Keep OpenAI tooling as an optional development accelerator; do not promote it into the product runtime unless a future OpenSpec change explicitly approves that.

## 2026-05-01 (UTC) — Codex (Cursor rules + agent-run contract)

- Completed: Added Cursor project rules in `.cursor/rules/` for session startup, bounded harness slices, and provider boundaries. Added provider-neutral `AR_2026_05` agent-run schema in `shared/src/agentRunSchemas.ts`, JSON Schema under `schemas/agent_run/`, and docs under `docs/contracts/agent-run/` plus `docs/agent/cursor-agent-harness.md`.
- Verified: Wrote failing test first, then passed `npm test --workspace backend -- --runTestsByPath tests/agent_run_schema.test.ts`; `npm run build --workspace shared`; `bash scripts/ci_docs_contract_sweep.sh`; `bash scripts/ci_linkcheck_fluency_docs.sh`; `npm run validate:agents`.
- Next focus: Add a Cursor CLI adapter only after `cursor-agent` is available locally and the desired event capture path is confirmed.

## 2026-05-01 (UTC) — Codex (Glean readiness roadmap)

- Completed: Executed the Glean readiness integration roadmap across phases 1-8 on `codex/OrgFluency-glean-readiness-execution`: source fixture adapter, source-to-readiness CLI, readiness-to-UT bridge, EvidenceBundle derivation, MCP readiness tools, stakeholder demo package, CI/example validation, and live-data decision gate.
- Verified: Targeted backend and MCP tests, MCP build, shared build, EvidenceBundle schema validation, docs contract sweep, scoped docs linkcheck, OpenSpec validation for each new change, backend CI (`91` suites / `482` tests), and harness verify (`138` passed, `3` skipped).
- Next focus: Review and archive approved Glean readiness OpenSpec changes; do not implement live Glean export ingestion or Glean-hosted MCP/read access until the Phase 8 gate evidence is confirmed.

## 2026-05-01 (UTC) — Codex (executive readiness prototype)

- Completed: Added `docs/integrations/glean/prototypes/executive-readiness-demo.html`, a static synthetic-data executive prototype with first-screen graphs, readiness/evidence/unlock/agent click paths, strict no-raw-record language, README/demo-guide links, and `scripts/validate_glean_executive_prototype.mjs`.
- Verified: `node scripts/validate_glean_executive_prototype.mjs`; in-app browser visual/click QA for overview, readiness filter, unlocks, and Agent Brief; `bash scripts/ci_docs_contract_sweep.sh`; `bash scripts/ci_linkcheck_fluency_docs.sh`; `git diff --check`; `./harness/scripts/verify.sh` (`138` passed, `3` skipped).
- Next focus: Review the prototype narrative with executives before expanding it beyond synthetic demo data; continue queue focus at `phase-03-fsc-min-signal` unless the human asks for another bounded Glean slice.

## 2026-05-07 (UTC) — Cursor (Glean trust-layer deep dive)

- Completed: Created branch `cursor/glean-trust-layer-deep-dive-c7e6`, added `artifacts/plan_glean_trust_layer_deep_dive.md`, and produced `artifacts/glean_trust_layer_deep_dive_2026-05-07.md` positioning FluencyTracr as an independent Glean Trust Evidence Plane.
- Verified: `git diff --check main...HEAD` passed after whitespace cleanup. Research/reporting slice only; no runtime code touched.
- Next focus: If approved, convert the recommendations into docs/OpenSpec slices: AWARE crosswalk, Glean Trust Launch Pack, `WorkflowRun`-only live adapter proposal, and MCP Boundary Ledger contract.

## 2026-05-07 (UTC) — Cursor (Glean Value Evidence execution plan)

- Completed: Added `artifacts/glean_value_evidence_execution_plan_2026-05-07.md`, an executable plan for a Glean Value Evidence Pack across live/current surfaces: Chat/Search/AI Answers, Skills, Auto Mode Agents, content-triggered agents, MCP/actions, embedded hosts, artifacts, and Protect/runtime controls.
- Verified: `git diff --check` passed for the documentation-only planning slice. Queue unchanged; plan proposes human-owned `glean-value-*` queue items and harness checklist candidates.
- Next focus: Human decision on whether to add the proposed `glean-value-*` queue items, then start with OpenSpec `add-glean-value-evidence-pack`.

## 2026-05-07 (UTC) — Cursor (Glean Value Evidence Pack contract)

- Completed: Implemented first build slice from the plan: OpenSpec `add-glean-value-evidence-pack`, `docs/contracts/glean-value-evidence/README.md`, synthetic `org-northstar-value-pack.json`, shared `GleanValueEvidencePackSchema`, export from `@learnaire/shared`, and `backend/tests/glean_value_evidence_pack_schema.test.ts`.
- Verified: `npm run build --workspace shared`; `npm test --workspace backend -- --runTestsByPath tests/glean_value_evidence_pack_schema.test.ts` (3 tests); `npx openspec validate add-glean-value-evidence-pack --strict`; `bash scripts/ci_docs_contract_sweep.sh`; `git diff --check`.
- Review: code-reviewer found claim-safety and doc/spec consistency issues; fixed with claim `.superRefine`, broader forbidden-field tests, README required field list, and `not_present` vocabulary alignment.
- Next focus: Slice 2 from the plan — OpenSpec + contracts for `add-glean-claim-registry` so customer-safe ROI/value language is governed separately from the pack shape.

## 2026-05-07 (UTC) — Cursor (Glean Claim Registry contract)

- Completed: Implemented Slice 2 from the plan: OpenSpec `add-glean-claim-registry`, `docs/contracts/glean-claim-registry/README.md`, default 10-template registry example, org-window evaluation example, shared `GCR_2026_05` registry/evaluation schemas, registry-aware validation/mapping into `GleanValueClaimReadinessSchema`, and `scripts/ci_glean_value_governance_gates.mjs` wired into docs sweep.
- Verified: `npm run build --workspace shared`; `npm test --workspace backend -- --runTestsByPath tests/glean_claim_registry_schema.test.ts tests/glean_value_evidence_pack_schema.test.ts` (15 tests); `npx openspec validate add-glean-claim-registry --strict`; `node scripts/ci_glean_value_governance_gates.mjs`; `python3 scripts/ci_v1_governance_gates.py`; `bash scripts/ci_docs_contract_sweep.sh`; `git diff --check`.
- Review: code-reviewer initially found ROI/template bypasses and pack drift; fixed with registry-aware validation, universal forbidden inputs, ROI hard-block from customer-safe language pending assumption governance, pack/evaluation comparison in the governance gate, and additional tests. Final blocker review found no blocking issues.
- Next focus: Slice 3 from the plan — Assumption Ledger contract so base rates, quality multipliers, recapture, hourly rates, confidence, and sensitivity can become governable evidence rather than ad hoc ROI inputs.

## 2026-05-07 (UTC) — Cursor (Glean Assumption Ledger contract)

- Completed: Implemented Slice 3 from the plan: OpenSpec `add-glean-assumption-ledger`, `docs/contracts/glean-assumption-ledger/README.md`, Time-Saves-seeded `default-assumption-ledger.json`, shared `GleanAssumptionLedgerSchema`, export from `@learnaire/shared`, targeted backend tests, and assumption validation in `scripts/ci_glean_value_governance_gates.mjs`.
- Verified: `npm run build --workspace shared`; `npm test --workspace backend -- --runTestsByPath tests/glean_assumption_ledger_schema.test.ts` (6 tests); `npx openspec validate add-glean-assumption-ledger --strict`; `node scripts/ci_glean_value_governance_gates.mjs`; `bash scripts/ci_docs_contract_sweep.sh`; `git diff --check`.
- Review: code-reviewer found a customer-safe loophole for low-confidence/high-sensitivity assumptions and README field omission; fixed by blocking `approval_state: customer_safe`, customer-facing language constraints, and customer visibility for risky assumptions, requiring `last_reviewed_at`, and adding tests. Final blocker review found no remaining blocker/important findings.
- Next focus: Slice 4 from the plan — promote Skills and Auto Mode Agents in readiness/value signal families with lifecycle fixtures and mappings.

## 2026-05-07 (UTC) — Cursor (Glean Skills + Auto Mode readiness)

- Completed: Implemented Slice 4 from the plan: OpenSpec `expand-glean-readiness-for-skills-agents`, `agent_run` and `skill_lifecycle` source readiness support, Auto Mode Agent and Skill lifecycle source fixtures, generator default/arg updates, regenerated source-derived readiness map, and targeted adapter/CLI tests.
- Verified: `npm run build --workspace shared`; `npm run glean:readiness:sources`; `npm test --workspace backend -- --runTestsByPath tests/glean_source_readiness_adapter.test.ts tests/glean_source_readiness_cli_contract.test.ts`; `node scripts/validate_glean_readiness_examples.mjs`; `npx openspec validate expand-glean-readiness-for-skills-agents --strict`; `git diff --check`.
- Review: code-reviewer found arbitrary free-text leak risk in source notes/actions; fixed by replacing source text with closed `validation_note_code`, `action_code`, and enum governance reasons, plus privacy tests. Final blocker review found no blocking/important findings.
- Next focus: Slice 5 from the plan — metadata-only Glean AI Work Evidence adapter that maps approved Glean AI work surfaces into readiness and claim evaluation without raw content.
